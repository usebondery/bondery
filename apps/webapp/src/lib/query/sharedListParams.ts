/** Shared list query params used across Home, Interactions, and Person pages. */

export const SELECTABLE_CONTACTS = { limit: 200, offset: 0 } as const;

export const INTERACTIONS_TIMELINE = { limit: 50, offset: 0 } as const;
